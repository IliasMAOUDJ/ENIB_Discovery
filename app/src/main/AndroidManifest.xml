<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="enibdiscovery.enibdiscovery">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.MenuScanner" />
        <activity android:name=".controller.MenuJouer" />
        <activity android:name=".controller.GameEdm" />
        <activity android:name=".controller.GameElec" />
        <activity android:name=".controller.GameAuto" />
        <activity android:name=".controller.MenuInfo"/>
        <activity android:name=".controller.FormationENIB"/>
        <activity android:name=".controller.ProgrammeCours"/>
        <activity android:name=".controller.GroupeEni"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".controller.MenuPreScanner"></activity>
    </application>

</manifest>